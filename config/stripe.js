// import Stripe from "stripe";

// if (!process.env.STRIPE_SECRET_KEY) {
//   throw new Error("❌ STRIPE_SECRET_KEY is missing. Check .env file");
// }

// const stripe = new Stripe(process.env.STRIPE_SECRET_KEY);

// export default stripe;